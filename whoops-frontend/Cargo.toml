[package]
name = "whoops-frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14.0", features = ["advanced", "canvas", "smol", "tiny-skia", "wayland"], default-features = false }
whoops-core = { path = "../whoops-core" }
